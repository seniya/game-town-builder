# 사람 확인 대기 목록

Version: 1.0
Date: 2026-09-23
Status: 운영 중 — 정본이 아닌 진행 목록

## 1. 목적과 운영 원칙

사람이 실제 화면을 보고 판단해야 하는 확인은 개발 환경 밖에서 이루어지므로 즉시 받기 어렵다.
이 문서에 모아 두고, **일반 Task는 에이전트 검증(자동 테스트·headless 브라우저 관찰·계측)으로
완료한 뒤 다음 Task로 진행한다.** 사람의 확인은 모아서 한 번에 받는다.

- 완료 조건·수치의 정본은 계속 TASKS / MVP_SPEC이다. 이 문서는 새 규칙을 만들지 않는다.
- 에이전트의 관찰을 사람의 판단으로 기록하지 않는다. 각 항목은 확인 전까지 `대기`다.
- 확인 결과가 부정적이면 해당 Task를 `수정 필요`로 되돌리고 docs/state에 원인과 수정을 남긴다.
- **예외: TASK-022와 TASK-033의 재미 검증은 이 목록으로 미루고 진행할 수 없다** (AGENTS 8,
  TASKS 4). 두 지점에 도달하면 사람의 플레이 관찰을 받을 때까지 다음 Phase로 넘어가지 않는다.
  에이전트는 그 전에 관찰용 빌드·장면·질문지를 준비해 둔다.

## 2. 확인 방법

확인자는 **자기 환경에서** 저장소를 clone해 직접 실행한다. 에이전트는 확인자용 서버를 띄우지 않는다
(에이전트의 headless 자동 관찰용 임시 서버는 별개다).

```text
git clone https://github.com/seniya/game-town-builder.git && cd game-town-builder
pnpm install
pnpm dev                       → http://localhost:5173/
실제 창의 Chrome, 창 크기 1280 × 720 권장, 브라우저 확대 100 %
```

항목마다 장면 URL과 볼 것을 적었다. 결과는 아래 표의 `결과` 칸에 날짜·확인자·한두 문장으로
적고, 부정적이면 무엇이 어색했는지 구체 장면을 남긴다.

## 3. 대기 항목

| ID | 출처 | 장면 | 확인할 것 | 상태 | 결과 |
| --- | --- | --- | --- | --- | --- |
| HR-001 | TASK-007 | `/?scene=house&orbit=0&view=0`, `view=1`, `view=2` | 작은 집 하나로 사람이 살 것 같은 느낌이 드는가. 벽·바닥·문·창문·지붕이 읽히는가. 블록 색이 서로 어울리는가. 창문 너머 실내가 비치는가. 모서리가 AO로 어두운가 | 대기 | |
| HR-002 | TASK-008 | `/?orbit=0&view=0` (전경), `view=1`~`5` | 숲 / 채석장 / 물가 / 마을 터 / 어두운 외곽을 눈으로 구분할 수 있는가. 섬이 다시 오고 싶은 장소로 보이는가 | 대기 | |
| HR-003 | TASK-008, MVP_SPEC 36 | `/?orbit=0&view=0&dpr=1&measure=60` 을 연 뒤 약 70 초 후 개발자 도구 콘솔의 `[gtb] 측정 완료` 줄 | 실제 창에서의 평균 FPS·하위 1% FPS·최대 프레임·드로우콜. headless 값(60.0 / 59.5 / 16.8 ms / 181)과 비교 | 대기 | |
| HR-004 | TASK-006 | `/?scene=mesh-edit&orbit=0&eps=20` | 청크 경계에서 블록이 빠르게 바뀌어도 화면이 끊기거나 경계에 틈이 보이지 않는가. TASK-013 이후 실제 설치 조작으로 다시 본다(`/?unlimited=1` 에서 빠르게 설치·파괴) | 대기 | |
| HR-005 | TASK-010~015 | `/` (섬, 조작 모드), `/?scene=house` | 캔버스 클릭으로 포인터 락이 걸리고 Esc 로 풀리는가. 마우스 시선·WASD·Shift·Space 가 자연스러운가(감도 0.0025 rad/px). 오른쪽 어깨 카메라에서 캐릭터가 조준점을 가리지 않는가. 집 안에서 카메라가 벽 밖으로 나가지 않는가. 조준 테두리가 잘 보이는가. E 로 인벤토리를 열고 E 로 닫으면 바로 조작으로 돌아가는가(Esc 로 닫으면 메뉴). 모달을 닫은 뒤 키가 눌린 채 남거나 닫는 클릭이 블록을 부수지 않는가 | 대기 | |
| HR-006 | TASK-013 / 015, MVP_SPEC 39 Test 1 | `/` 에서 나무 → E 제작 → 설치 → 파괴 | 파괴 시간(흙 0.6 / 돌 1.5 / 돌벽돌 2.0 초)과 균열 8 단계가 자연스러운가. 우클릭 설치 위치가 예측대로인가. 침대·문 방향이 보는 방향과 맞는가. 핫바·제작 패널이 읽기 쉬운가. 물가에서 물에 들어갔다 되돌아오는 것이 거슬리지 않는가 | 대기 | |
| HR-007 | TASK-020 / 021 | `/?scene=room-lab&debug=1` 에서 빈 땅에 방을 짓고 부수기 | 방 인식 순간 경계가 빛나고 이름이 떠오르는 것이 분명하고 짧게 느껴지는가. 해제 때 붉은 깜빡임과 경고음이 거슬리거나 놓치기 쉽지 않은가. 인식음·경고음이 서로 구별되는가(합성음, 최종 음질은 TASK-050). 라벨이 멀어질 때 자연스럽게 흐려지는가. F3 의 방 줄 `이번 프레임 … (최대 …)` 가 실제 창에서 3 ms 안인가(headless 는 20 편집/s 에서 최대 3.0 ms) | 대기 | |

## 4. 재미 검증 게이트 — 이 목록으로 미루지 않는다

아래는 일반 대기 항목과 다르다. **사람의 플레이 관찰 결과를 받을 때까지 다음 Phase 로 넘어가지 않는다**
(AGENTS 8, TASKS 2.1 / 4). 에이전트가 관찰용 빌드·장면·질문지를 준비해 두었다.

### G1. TASK-022 방 만들기 (Phase C → D 분기점) — 상태: 관찰 대기

준비된 빌드: `main` 의 Phase C 기록 커밋. 확인자 환경에서 2장대로 clone 후 `pnpm install && pnpm dev` → Chrome 실제 창 1280 × 720 권장, 소리 켜기.

플레이 순서(약 20~30 분). 디버그(F3)는 켜지 않는다. 진행이 막힐 때만 관찰자가 알려 준다.

```text
1  /?scene=room-lab          넓은 풀밭. 방 재료가 인벤토리에 있다(판자·문·침대·가구·창문·횃불)
   조작: 캔버스 클릭 → WASD / 마우스 / 좌클릭 파괴 / 우클릭 설치 / 1~9 선택 / E 인벤토리 / Tab 방 진단
2  가운데 빈 땅에 5 × 5 방을 직접 짓는다 (Test 2: 바닥 → 벽 두 칸 → Tab → 문 → 침대 → 벽 부수기 → 복구)
3  북쪽 줄의 사례를 Tab 으로 본다 (서쪽부터)
     A 완성된 빈 방  /  B 문 없는 방  /  C 벽 한 칸 빠진 방(서쪽 벽)  /  D 흙벽 공간
   C 는 표시만 보고 구멍을 찾아 막아 본다 (Test 3)
4  남쪽 줄의 사례를 Tab 으로 본다
     E 침대 + 화덕·물통(주방이 우선) / F 벽 한 칸의 둘째 층 빠짐 / G 방 안 1 칸 높이 판자 단(같은 높이 제한)
5  원하면 기존 블록·가구로 방을 더 꾸민다 (창문·횃불·돌벽돌·식탁과 의자). 시간 제한 없음
6  (선택) / 섬에서 처음부터 나무 → 판자 → 방 한 칸
```

질문지 — 각 질문에 예/아니오/애매 + 그렇게 느낀 구체적인 장면을 적는다.

```text
시스템 축 (판정·진단)
Q1  5 × 5 방을 만들고 인식시키는 과정이 즐거웠는가
Q2  인식되지 않았을 때 이유를 진단(Tab)만으로 알 수 있었는가. 어느 사례에서 헷갈렸는가
Q3  실패가 짜증이 아니라 퍼즐로 느껴졌는가
Q6  흙집(D)·주방에 침대 추가(E)·같은 높이 제한(G)의 이유를 이해했는가. 규칙이 납득되는가
Q7  인식 순간(빛·이름·소리)이 분명했는가. 해제(붉은 깜빡임·경고음)는 알아챘는가

감성 축 (공간·표현)
Q4  기존 블록과 가구로 내 취향에 맞게 공간을 더 가꾸고 싶은가
Q5  기능이나 보상과 상관없이 방이나 집을 조금 더 예쁘게 꾸미고 싶은가
Q8  작은 방의 실루엣·블록 팔레트·가구 배치 중 무엇이 좋았고 무엇이 아쉬웠는가. 더 꾸미고 싶었던 이유
```

기록: 결과는 docs/state 에 TASKS 4.1 의 022 형식(빌드·장면·관찰자·응답·두 축 판정·개선 대상)으로
남긴다. 부정적이면 판정·진단 문제와 감성·표현 문제를 나눠 개선하고 다시 관찰한다. 방 조건을
바꿔야 하면 MVP_SPEC·관련 ADR·완료 조건을 먼저 개정한다. 콘텐츠를 추가해서 해결하지 않는다.

| 날짜 | 관찰자·플레이어 | 시스템 축 | 감성 축 | 요약 |
| --- | --- | --- | --- | --- |
| | | 미검증 | 미검증 | |

## 5. 완료 항목

아직 없다.
