# TASK-037 해금 — 완료

Date: 2026-09-24
Status: **완료**
관련 정본: MVP_SPEC 23.2 / 23.5, ARCHITECTURE 17, TASK-015 의 `data/unlocks.ts`

## 변경한 것

- `GameWorld`: CraftingSystem 의 레벨 조회를 시작 레벨 상수에서 `VillageLevelSystem.level` 로 바꿨다.
- `ui/InventoryPanel.ts`: VILLAGE_LEVEL_UP 에도 다시 그려, 열린 패널에서 해금이 즉시 보인다.

## AC

| AC | 결과 | 근거 |
| --- | --- | --- |
| 레벨 1 에서 window / chest 회색 | 통과 | 테스트 + 브라우저("마을 레벨 2 필요") |
| 회색 항목에 필요 레벨 | 통과 | 테스트(requiredLevel 2 / stone_brick 3) + 브라우저 |
| 레벨 2 달성 즉시 활성화 | 통과 | 테스트(ring 직후 제작 성공) + 브라우저(열린 패널에서 표시가 바로 바뀜) |
| 디버그 모드에서도 잠긴 블록 제작 불가 | 통과 | 테스트(무제한 모드에서 craft → locked) |
| 레벨이 오른 뒤 다시 잠기지 않음 | 통과 | 테스트(침대 제거로 게이트가 깨져도 해금 유지) |
| cooking_stove / water_pot 레벨 1 제작 | 통과 | 테스트 |

## 검증

- `pnpm test` 391 통과(신규 unlock 3), lint(src·tests)·typecheck·build 통과.
- headless Chrome `?scene=kitchen-lab`: E 로 연 패널에 "마을 레벨 2 필요"·"마을 레벨 3 필요" → ring 뒤 "마을 레벨 2 필요" 사라짐.

## 다음 단계

1. TASK-038 새 주민 도착
2. 사람 확인: HR-018 이하 대기 목록
