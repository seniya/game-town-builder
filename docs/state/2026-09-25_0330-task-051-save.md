# TASK-051 저장 — 완료

Date: 2026-09-25
Status: **완료**. 사람 확인 HR-027 대기
관련 정본: ARCHITECTURE 23, [ADR 038](../adr/038-save-slot-autosave-and-load.md)

## 변경한 것

| 영역 | 파일 |
| --- | --- |
| SaveData(버전 1)·captureSave(structuredClone)·migrate(거부)·applySave(23.4 순서) | `src/game/save/saveData.ts` |
| 자동 저장 요청(07:00·종 직후)·순서·최신 합치기·실패 알림 | `src/game/systems/SaveSystem.ts` (16 번 슬롯) |
| IndexedDB 슬롯(트랜잭션 교체·읽기·지우기) | `src/ui/IndexedDbSaveStore.ts` |
| 변경 청크 추적(세션 전체)·청크/배치 복원, PlacementIndex.clear | `voxel/VoxelWorld.ts`, `voxel/PlacementIndex.ts` |
| 주민 복원(id·역할·위치·체력·기절·식사, 번호 이어 쓰기) | `GameWorld.restoreResidents` |
| 이어하기(섬만, ?new=1 무시)·"저장됨 / 저장 실패" 표시·메뉴 "새로 시작" | `main.ts`, `ui/SaveIndicator.ts`, `ui/PauseMenu.ts` |
| 07:00 전에 시작하면 그날 아침 저장·피해 보고를 건너뛰던 문제 수정 | `SaveSystem.ts`, `RepairSystem.ts` |

## AC

| AC | 결과 | 근거 |
| --- | --- | --- |
| 새로고침 뒤 블록 그대로 | 통과 | 테스트 + 브라우저(판자 두 칸) |
| 방 재인식(저장하지 않음) | 통과 | 테스트(침실 1) |
| 주민 위치·침대 배정·진행 상태 | 통과 | 테스트 |
| 감사 포인트·레벨·해금 | 통과 | 테스트 + 브라우저(포인트) |
| WorldState 는 계산 | 통과 | 테스트(키 없음, 값 같음) |
| 로드 5 초 이하 | 통과 | 테스트(섬 생성 + 적용 5 초 미만) |
| 변경된 청크만 저장 | 통과 | 테스트(전체의 1/4 미만) |
| Date.now() 없음 | 통과 | 테스트(epoch 형식 정수 없음, gameMinutes 만) |
| 버전이 다르면 거부 | 통과 | 테스트(SaveRejectedError) |
| gameMinutes 하나·다중 칸 배치·objectId | 통과 | 테스트(문 배치 동일, 다음 id 가 겹치지 않음) |
| 습격 중 몬스터 체력·도달 집합·파괴 예산·종료 이벤트 | 통과 | 테스트(05:00 에 한 번 RAID_ENDED, reached 1) |
| 종 직후 도착 예약 보존·중복 생성 없음 | 통과 | 테스트 |
| 자정 전후 취침 보상·기절·식사 구간·당일 수리 8 칸 | 통과 | 테스트 |
| 현재 목표·미완료 대화 표시 | 통과 | 테스트 |
| 로드의 방 재구축은 보상 이벤트 없음 | 통과 | 테스트(이벤트 0, 포인트 불변) |
| 저장만 하면 Action·예약 유지, 로드 때만 비움 | 통과 | 테스트(자는 Action 동일 객체, 로드 뒤 idle·예약 0) |
| 비동기 저장 중 편집·연속 요청·실패 시 이전 슬롯 | 통과 | 테스트(가짜 저장소) |
| 침대·의자 사용 중 저장 → 논리 위치가 가구 안이 아님 | 통과 | 테스트 |
| 목표 id·원천·집계 종류·대사 순서·과거 대사 무시 | 통과 | 테스트 |

## 검증

- `pnpm test` 460 통과(신규 save 10), lint·typecheck·build 통과.
- headless Chrome `/`: 판자 두 칸·포인트 42 를 저장(요청) → "저장됨" → 새로고침 → 판자 유지, "저장한 곳에서 이어 합니다", 포인트 42(+ 새로 발생한 ARRIVAL 15).

## 다음 단계

1. TASK-052 엔딩
