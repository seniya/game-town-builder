# STYLE-003 가구·소품 모형과 아이콘

Date: 2026-09-25 23:10

## 대상과 상태

- 대상: TASK-STYLE-003 / 상태: **완료(에이전트 검증)**. 사람의 판단은 게이트 V2(HR-014·029 의 "특징이 없다" 를 여기서 다시 본다).
- 명세 MVP_SPEC 45.6, 결정 ADR 047.

## 한 일

- `render/furnitureModels.ts`(신설): 식탁·의자·상자·화덕·물 항아리·종·횃불·침대·문 둥근 모형. 불꽃은 `unlit = 2` 로 스스로 빛난다.
- `render/FurnitureView.ts`(신설): 종류별 InstancedMesh + 외곽선, 칸 목록은 BLOCK_CHANGED 로 갱신, 의자 방향 `chairTurns`.
- `blocks.ts` PROPS 에 일곱 블록 추가(렌더 분류). `blockShapes.ts` 에는 창문만 남겼다(`CHAIR_SEAT_HEIGHT` 는 그대로).
- `PropView`(침대·문)·`CropView`(작물)를 같은 둥근 모형·툰 재질로 바꿨다.
- `render/modelIcons.ts`(신설): 가구 아이콘을 게임 렌더러로 모형에서 한 번 그린다. 씨앗·작물·음식 아이콘은 둥근 벡터 그림.
- 툰 재질: `unlit` 2 이상이면 제 색의 3 배(빛 번짐 문턱 1.0 을 넘는다). 시안 장면도 FurnitureView 로 가구를 그린다.

## 측정 (headless, 1280 × 720, DPR 1, 10 초)

| 장면 | 평균 FPS | 최대 드로우콜 (STYLE-002 뒤 → 지금) |
| --- | --- | --- |
| `/?orbit=0&view=0` | 60.0 | 445(STYLE-005) → 351 |
| `/?scene=kitchen-lab&orbit=0` | 60.0 | 116 → 94 |
| `/?scene=mvp-perf` | 60.0 | 302 → 252 |

## 관찰 (에이전트)

- 주방: 화덕(네 면 아궁이·불·냄비의 국)과 물 항아리(물빛·국자)가 모형으로 보인다. 횃불 불꽃이 번진다. 문에 작은 창과 금 손잡이.
- 핫바: 침대·문·횃불·식탁 아이콘이 모형 그림이다.
- 확인하지 못한 것: 점심 식당에서 의자에 앉은 주민(headless 에서 플레이어를 식당 안에 옮기지 못함). V2 질문지에 넣는다.

## 다음 할 일

- STYLE-007: 몬스터 모형(둥근 악당)과 UI 톤. 45.7 을 먼저 적는다.
