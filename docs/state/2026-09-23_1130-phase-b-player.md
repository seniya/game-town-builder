# Phase B 플레이어 구현 — TASK-009~016

Date: 2026-09-23
Status: Phase B 8개 Task 완료 — 실제 창의 포인터 락·조작감 확인은 사람 확인 대기(HR-005)
구현 기준 커밋: 7dd1c3d (시작) → 0e2bcc3, 227f60e, e3120da, 924377b, 615263e, 74eb127, 이 기록의 커밋
관련 정본: MVP_SPEC 9 / 9.5 / 10 / 10.4.1 / 13 / 14 / 23.2 / 29.0 / 34, ARCHITECTURE 3 / 5 / 8 / 15 / 26 / 27
해결한 READY: READY-06, READY-02, READY-03

## 판단

TASKS 2.3의 순서 009 → 010 → 012 → 011 → 014 → 013 → 015 → 016 으로 진행했고 각 Task의 AC를
통과했다. Phase B 끝의 상태("돌아다니며 채집·제작하고 블록을 부수고 놓을 수 있다")에 도달했다.
MVP_SPEC 39 Test 1(블록)의 최초 전체 검증(TASKS 2.6)을 브라우저에서 디버그 없이 통과했다.
새 블록·가구·자원·메커닉·프레임워크는 추가하지 않았다.

자동 테스트 170개, lint·typecheck·build 통과. 브라우저 확인은 Phase A와 같은 headless Chrome 148 +
CDP 방식이다. headless 에서는 포인터 락이 걸리지 않으므로 락 경로는 InputSystem·상태 기계를 직접
조작해 확인했고, 사람이 실제 창에서 조작한 결과는 아직 없다.

## Task별 결과

| Task | 상태 | 변경한 것 | AC |
| --- | --- | --- | --- |
| 009 | 완료 | `voxel/collision.ts` 축 분리 스윕·step-up(지면에서만, 머리 공간 검사)·0.4 분할, 월드 밖 고체 | 전부 통과 (가짜 월드 테스트 14) |
| 010 | 완료 | `InputSystem`(원시 입력→InputFrame), `ui/domInput.ts`, `entities/Player.ts`, `PlayerMovementSystem`(걷기·달리기·점프·중력·물 복귀·끼임 해소) | 통과. 실제 창의 포인터 락은 HR-005 |
| 012 | 완료 | `voxel/raycast.ts` Amanatides & Woo, `systems/aim.ts` 어깨점 조준, `render/Highlight.ts` | 전부 통과 (샘플링 기준 구현과 대조) |
| 011 | 완료 | `render/CameraController.ts`, `aim.cameraBoomDistance`(고체·door 에 막힘) | 전부 통과 (벽·window·door·천장 테스트, 집 안 관찰) |
| 014 | 완료 | `InventorySystem`(36칸·64·원자적 add/remove/exchange/swap), `ui/Hotbar.ts`, `render/itemIcons.ts`, `EventBus.transaction` | 전부 통과 |
| 013 | 완료 | `BlockEditSystem` 파괴 진행·드롭·설치 규칙 10.4·다중 칸·원자 커밋, `render/crackTexture.ts` | 전부 통과 (21 테스트) |
| 015 | 완료 | `data/recipes.ts` 12종, `data/unlocks.ts`, `CraftingSystem`, `ui/InventoryPanel.ts`, `ui/ModalController.ts`, `ui/PauseMenu.ts` | 통과. 실제 창의 락 복귀는 HR-005 |
| 016 | 완료 | `DebugSystem`, `ui/DebugPanel.ts`(F3), 블록 무제한 모드, `?debug=1` / `?unlimited=1` | 전부 통과 (프로덕션 빌드 preview 에서 확인) |

### 결정 (ADR 022, 정본 반영)

- READY-06 → MVP_SPEC 9.5: 물 진입 시 마지막 안전 지면으로 복귀. 해안 수심 1 칸이 해변보다 2 칸 낮아
  통과시키면 갇힌다는 섬 데이터(island.ts)를 근거로 했다. 수평 월드 밖·y<0 은 충돌 고체.
- READY-02 → MVP_SPEC 10.4.1: 지지 조건은 설치 때만. 지지면을 부숴도 위 객체는 남는다.
- READY-03 → MVP_SPEC 29.0, ARCHITECTURE 3: 조작 중 / 모달 / 메뉴. 메뉴만 일시정지.
- MVP_SPEC 9.3 / 10.1 / 34: 피치 제한의 부호(음수가 내려다보기), 시선 원점 1.6·어깨 0.6, 마우스 감도,
  stepUpHeight, maxFrameSeconds 를 balance 에 추가했다. 게임 수치를 바꾼 것은 없다.
- ARCHITECTURE 5 / 15 / 26 / 27: EventBus.transaction, PlayerMovementSystem 책임, 플레이어 상태 소유, F3 범위.

## 검증

### 명령

```text
pnpm test        170 passed (17 files)
pnpm lint        통과
pnpm typecheck   통과 (015 커밋에서 테스트 파일 타입 오류 1건을 놓쳤고 016 커밋에서 고쳤다)
pnpm build       통과 (three 포함 번들 >500kB 경고만)
```

### 브라우저 관찰 (headless Chrome 148, ANGLE Vulkan, 1280 × 720)

- 조작 모드(기본 `/`, `?scene=house`): W 로 카메라 기준 앞으로 걷고 D 로 옆걸음. CDP 키 이벤트가
  DOM 어댑터를 거쳐 들어간다. 종(1칸)에 자동으로 올라섰다가 내려온다.
- 작은 집 안에서 시선을 돌려도 카메라가 벽·지붕 안쪽에 머문다. 캐릭터는 조준점 왼쪽에 보인다.
- 조준 테두리(1px 어두운 선)가 대상 블록을 감싼다. 밝은 판자 바닥에서 조금 약하게 보인다(HR-005).
- 핫바 아이콘은 아틀라스 타일로 그린 작은 입방체다. 숫자키 3 으로 선택이 바뀌고 이름이 잠깐 뜬다.
- 균열이 파괴 40% 에서 통나무 면에 보인다.
- MVP_SPEC 39 **Test 1** (디버그 없음, 한 세션):
  1 나무 통나무를 좌클릭 유지로 파괴(1.0 s) → 2 log 1 개 획득 → 3 E 로 패널을 열고 [만들기] 로
  plank 4 → E 로 닫기(headless 는 락 요청이 거부되어 명세대로 메뉴로 감, 메뉴 클릭으로 복귀)
  → 4 우클릭으로 plank 설치(3 개 남음) → 5 다시 파괴해 4 개 → 6 발 칸·머리 칸 설치는 `character` 로 거부.
- F3: 프로덕션 빌드(`vite preview`)에서 FPS 60 · 프레임 16.7 ms · 드로우콜 91 · 청크 256/256 표시.
  `?unlimited=1` 에서 빈 핫바로 설치된다. preview 첫 로드에 WebGL 컨텍스트 손실/복구 로그가 한 번 있었고
  (headless GPU), 이후 정상 렌더였다. 원인은 확인하지 않았다.

### 측정

이번 Phase 에 새 성능 측정은 없다. F3 의 FPS 는 headless 60 Hz 상한이라 여유를 알 수 없다.
연속 설치 체감(HR-004)은 이제 실제 조작으로 볼 수 있다.

## 아직 없는 연동과 후속 확인

- 설치 점유 검사의 NPC·몬스터 몸체: 포트만 있고 지금은 플레이어뿐이다 (TASK-028 / 045 에서 연결).
- 블록 편집의 Nav·방 후보 즉시 무효화: TASK-020 / 024.
- farmland 제거 시 crop 제거: TASK-030 (FarmSystem).
- 파괴 완료 파티클·드롭 연출, 블록별 효과음(MVP_SPEC 30 / 31): 이번 AC 밖이라 하지 않았다.
- 종·저장소·대사·피해 보고 모달: 상태 기계에 이름만 있고 036 / 040 / 049 / 051 에서 연결한다.
- 모달 중 기절 시 모달 닫기: TASK-046.
- 마을 레벨: CraftingSystem 은 시작 레벨 1 을 읽는다. VillageLevelSystem(TASK-037) 에서 연결한다.
- 저장 대상(인벤토리·플레이어 위치): TASK-051. `InventorySystem.restore` 만 준비했다.

## 실제 소요와 재작업

- 한 세션 약 35분(문서 확인·정책 확정 포함). 에이전트 1명, 사람의 개입 없음.
- 재작업: 테스트 시나리오 오류 4건(점프 착지 시점, 물 복귀 관찰 시점, 선택 칸이 비는 순서, 가득 찬
  인벤토리 준비), 엔티티 계층의 voxel import(AabbBody 를 types 로 이동), 제작 패널 열 폭,
  디버그 패널 선택 표시 불일치, 015 커밋의 테스트 타입 오류. 모두 같은 세션에서 해결했다.
- 예상 작업량 갱신(ROADMAP 3): Phase C(6 Task)의 구현은 1~2 세션. TASK-022 는 사람 플레이 관찰과
  개선 반복이 필요하며 그 재작업량은 관찰 전에는 추정하지 않는다.

## 다음 할 일

1. TASKS 2.3 의 다음 묶음 017 → 018 → 019 → 020 → 021 → 022 ★. 새 READY 선행 항목은 없다.
2. TASK-022 에 도달하면 AGENTS 8 에 따라 관찰용 빌드·장면·질문지를 준비하고 **사람의 플레이 관찰을
   받을 때까지 Phase D 로 넘어가지 않는다.**
3. 사람 확인 대기: [HUMAN_REVIEW](../project/HUMAN_REVIEW.md) HR-001~006. 기다리지 않고 진행한다.
