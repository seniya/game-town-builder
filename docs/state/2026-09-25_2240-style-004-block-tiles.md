# STYLE-004 블록 텍스처·팔레트

Date: 2026-09-25 22:40

## 대상과 상태

- 대상: TASK-STYLE-004 / 상태: **완료(에이전트 검증)**. 사람의 판단은 게이트 V2.
- 명세 MVP_SPEC 45.5, 방향 ADR 044·046.

## 한 일

- `render/blockTiles.ts`(신설): 22 종 × 면 3 의 64 px 손그림풍 타일. 시안의 칠하기 도구(`style/styleTiles.ts`)를 공용으로 열어 쓴다.
  돌(세 단계 얼룩·둥근 금), 모래, 나무 껍질(물결 결)·나이테, 물(반투명 잔물결), 창문(둥근 창틀·십자 살·반짝이는 유리),
  문·침대·화덕(아치 아궁이)·물 항아리·식탁·의자·상자(쇠 띠·자물쇠)·밭(부드러운 고랑)·종·횃불·새싹.
- `render/atlas.ts`: 16 px 픽셀 아틀라스 → **DataArrayTexture**(층마다 밉맵). `render/palette.ts` 는 더 쓰지 않아 지웠다.
- 복셀 셰이더: `textureGrad` 로 밉맵 단계를 고르고, 월드 좌표 저주파 잡음으로 밝기를 흔들어 반복 무늬를 숨긴다.
- 물 반사가 옅은 지평선색에 바래 얕은 물가가 회색으로 보이던 것을 물빛과 섞어 줄였다(STYLE-005 보정).
- 아이템 아이콘은 새 타일을 부드럽게 줄여 그린다.

## 검증

- 시험 `tests/blockTiles.test.ts` 5 개(크기·결정성·투명 규칙·면별 차이·텍스처 배열 층). 타일 시트를 이미지로 뽑아 22 종을 눈으로 확인했다(에이전트).
- headless: 섬 숲·채석장·물가, 주방 안 방 경계 표시(`bounds=1`)가 새 텍스처 위에서 읽힌다.

## 다음 할 일

- STYLE-002: 주민 넷·플레이어를 둥근 SD 모형(강체 스키닝 한 메시)으로 바꾼다. 45.4 를 먼저 적는다.
