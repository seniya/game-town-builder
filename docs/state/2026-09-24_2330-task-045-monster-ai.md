# TASK-045 몬스터 AI 와 블록 파괴 — 완료

Date: 2026-09-24
Status: **완료**. 사람 확인 HR-021 대기. 플레이어·주민 공격은 TASK-046
관련 정본: MVP_SPEC 24.2 / 24.3 / 24.4, ARCHITECTURE 4.4 / 18, ADR 015 / 025

## 변경한 것

| 영역 | 파일 |
| --- | --- |
| 판단(종 반경 6 경로 → 이동 / 이어 탐색 대기 / NO_PATH → 경계 파괴 후보 → 접근 → 파괴 / 배회)·도달 기록 | `src/game/systems/MonsterSystem.ts` (9 번 슬롯) |
| 파괴: breakSeconds × 2, 예산(16) 차감 후 편집 API(by 'monster'), 두 칸 객체는 통째로 | 같은 파일, `RaidSystem.spendDestroyCells` |
| 파괴 연출: 부수는 칸에 흔들리는 균열 상자(몬스터마다 단계) | `src/render/MonsterView.ts` |

## AC

| AC | 결과 | 근거 |
| --- | --- | --- |
| 열린 마을에서 종까지 걸어온다 | 통과 | 테스트 + 브라우저(모서리 → 종, 우회) |
| 판자벽 앞에서 멈추고 부순다 | 통과 | 테스트(고리 벽 칸, 벽 바로 바깥에서) |
| plank 1.6 초 | 통과 | 테스트(파괴 시작 → BLOCK_CHANGED 1.6 초) |
| 부서지면 통행 갱신 → 들어온다 | 통과 | 테스트(두 칸 부순 뒤 목표 반경 안) |
| dirt / stone 은 부수지 않고 우회 | 통과 | 테스트(틈 우회, 완전히 막히면 배회·파괴 0) |
| NODE_LIMIT 에서는 부수지 않는다 | 통과 | 테스트(프레임당 5 노드 스케줄러로 20 프레임 대기·정지·파괴 0) |
| 완전히 막히면 배회 → 05:00 소멸 | 통과 | 테스트(배회) + TASK-044(소멸) |
| 종 반경 6 도달 기록 | 통과 | 테스트 |
| 종 불파괴·열린 마을에서 목표 반경까지 걸음 | 통과 | 테스트(종 블록 유지, 파괴 0) |
| 벽에서 떨어져 스폰해도 벽 앞까지 이동 후 파괴 | 통과 | 테스트(모서리 스폰) |
| 파괴량 16 이하·두 칸 객체 부분 파괴 없음 | 통과 | 테스트(문 = 2 칸 통째로, 예산 1 이면 문을 부수지 않음) |
| 흙벽 제한의 재미 관찰 기록 | 기록함 | 아래 관찰 + HR-021 |

## 관찰 (에이전트, headless — 사람의 판단이 아니다)

- `?scene=kitchen-lab` 21:00: 세 마리가 두 모서리에서 나와 주방 건물을 돌아 종 쪽으로 걸었다(21:35 에 종 부근). 빛나는 눈 덕에 밤에도 위치가 보인다.
- 흙·돌을 부수지 않으므로, 플레이어가 흙으로 마을을 모두 두르면 몬스터는 배회하다 사라진다. 방어가 "흙 두르기" 한 가지 답으로 줄어들 위험이 있다.
  반면 방·집은 판자·돌벽돌로 짓기 때문에 마을 전체를 흙으로 두르는 수고와 미관이 대가다. 재미를 해치는지는 HR-021 에서 사람이 판단한다(규칙 변경은 MVP_SPEC 개정이 먼저).

## 검증

- `pnpm test` 425 통과(신규 monster 6), lint·typecheck·build 통과.

## 다음 단계

1. TASK-046 전투 — 구현 전 READY-05(공격 거리·대상 선택·기절 회복·안전한 부활 위치) 확정
