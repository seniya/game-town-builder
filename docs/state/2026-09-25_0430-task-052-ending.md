# TASK-052 엔딩 — 완료

Date: 2026-09-25
Status: **완료**. 사람 확인 HR-028 대기
관련 정본: MVP_SPEC 27.1 / 28 Phase 6 / 39 Test 10, ARCHITECTURE 20 / 23, [ADR 039](../adr/039-ending-cutscene-modal.md)

## 변경한 것

| 영역 | 파일 |
| --- | --- |
| 엔딩 상태(pending / played, ENDING_FINISHED, 저장) | `src/game/systems/EndingSystem.ts`, `GameWorld.ending`, `save/saveData.ts` |
| 샷 구성·문장·카메라 거리 | `src/game/data/ending.ts` |
| 샷 계산(순수 함수, three 없음) | `src/game/ending/endingShot.ts` |
| 연출 화면(검은 띠·문장·암전·건너뛰기) | `src/ui/EndingOverlay.ts`, ModalName `ending` |
| 카메라 적용 | `CameraController.showPose` |
| 연결: 엔딩 먼저·피해 보고는 뒤, 일시정지 중 저장 요청 처리, 연출 뒤 알림 | `main.ts`, `ui/ArrivalToast.ts`, `SaveSystem`(ENDING_FINISHED 구독) |

연출: 아침 전경 궤도(6 초) → 목수를 내려다보며 돈다(6 초) → 주민 중심 위(6 초) → 종 둘레로 떠오르며 마지막 문장(7 초). 모두 실시간이며
그동안 게임 시간과 주민 행동은 그대로 흐른다.

## AC

| AC | 결과 | 근거 |
| --- | --- | --- |
| 2 차 습격 종료 뒤 첫 07:00 이후·주민 5·선행 완료에서 한 번, 등호 비교 없음 | 통과 | `tests/ending.test.ts`(06:59 없음 → 09:00 으로 뛰어도 한 번, 주민 4 면 없음), 기존 `gameEvents.test.ts` 경계 |
| 연출이 재생된다 | 통과 | headless Chrome `/?new=1`: 07:05 에 `ending` 모달, 네 컷 화면 캡처 |
| 연출이 끝나도 게임이 계속된다 | 통과 | 시험(시간 흐름·주민 행동·재발생 없음), 브라우저(25 초 뒤 닫힘, played) |
| 저장하고 이어서 할 수 있다 | 통과 | 시험(연출 중 저장 → 다시 보임, 본 뒤 저장 → 안 보임, 이벤트 재발생 없음), 브라우저(Esc 로 닫고 메뉴 → 저장 → 새로고침 뒤 played, Day 2) |

## 검증

- `pnpm test` 466 통과(신규 ending 6), typecheck·eslint(src·tests)·prettier·build 통과.
  `pnpm lint` 는 사용자가 새로 둔 `.claude/helpers/*.cjs` 때문에 실패한다(이 작업의 변경이 아니며 커밋하지 않았다).
- headless Chrome: 섬에서 레벨 3·주민 5·습격 1/2 결과를 주입하고 07:05 로 옮겼다. 연출 중 목수는 수리할 피해가 없어 `대기` 였다.
- 발견·수정: 연출을 Esc 로 닫아 메뉴로 가면(headless 는 락 재요청이 실패해 같은 경로) 일시정지 때문에 엔딩 저장이 처리되지 않았다 → 일시정지 중에도 SaveSystem.update 를 부른다.

## 아직 검증하지 못한 것

- 처음부터 끝까지의 실제 플레이로 엔딩에 도달하는 것(TASK-053·HR-028).
- 연출의 감정 효과·길이·카메라 가림(HR-028).

## 다음 단계

1. TASK-053 밸런스와 성능
