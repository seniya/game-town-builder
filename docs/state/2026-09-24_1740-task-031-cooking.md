# TASK-031 요리와 요리사 — 완료

Date: 2026-09-24
Status: **완료**. 사람 확인 HR-014 대기
관련 정본: MVP_SPEC 16(16.1 신설), ARCHITECTURE 13.4 / 14 / 15, [ADR 031](../adr/031-cooking-stove-claims-and-ingredient-reservation.md)
해결한 READY: 없음

## 변경한 것

| 영역 | 파일 |
| --- | --- |
| Kitchen 화덕 목록·조리 후보·화덕/재료 예약·완료 확정(crop −2, food +3) | `src/game/systems/CookingSystem.ts` (update 10 번, 판단 전 갱신) |
| 조리 Action(시작 때 재료 예약, 게임분 60 뒤 완료, 예약이 사라지면 failed) | `src/game/actions/CookAction.ts` |
| 판단: 요리사·역할 시간·재료 준비 → 화덕 접근 셀로 이동(`cook` 목적) → 조리 | `src/game/systems/NPCDecisionSystem.ts` |
| 실행: cook 계획, `cookStove` 예약 옮기기 | `src/game/systems/NPCSystem.ts`, `src/game/actions/Action.ts`, `src/game/GameWorld.ts` |
| 조리 자세(화덕을 보고 젓기) + 화덕 위 김 | `src/render/NpcView.ts`, `UsePose` 에 'cook' |
| F3 요리 줄·"작물 +2", 관찰 장면 `?scene=kitchen-lab`(fixture `startCrop`) | `DebugSystem.ts`, `DebugPanel.ts`, `visualFixtures.ts`, `main.ts` |

## AC

| AC | 결과 | 근거 |
| --- | --- | --- |
| Kitchen 이 인식되어 있어야 요리한다 | 통과 | 테스트(주방 없음 / 주방 생긴 뒤 후보 갱신) |
| crop 2 미만이면 요리하지 않는다 | 통과 | 테스트 |
| 1 게임시간 뒤 crop −2, food +3 | 통과 | 테스트(54 분에는 변화 없음) + 브라우저(09:47 시작 → 10:4x crop 4→2, food 3) |
| Kitchen 이 없으면 crop 이 쌓이기만 한다 | 통과 | 테스트(1.5 시간 진행, 대기, 시계 진행) |
| 요리 중 주방 해제 → 취소 | 통과 | 테스트(물통 제거로 해제, 화덕 파괴는 재판정 전 즉시) |
| 시작 때 예약·완료 때 소비, 중단·로드로 손실·복제 없음 | 통과 | 테스트(12:00 중단, resetForLoad, 완료 두 번 요청, 예약보다 crop 부족) |
| 식사를 못 했어도 다음 역할 시간에 요리 | 통과 | 테스트(hasEatenThisMeal false 로 13:00 재조리) + 판단 단위 테스트 |
| 저장소·시설·역할 시간 변경으로 후보 갱신·예약 재검증, 음식 부족 조건 없음 | 통과 | 테스트(crop 추가·주방 생성·12:00·crop 감소, food 99 에서도 조리) |
| 화덕을 쓰는 조리 동작이 이동·대기와 구별 | 통과(에이전트 관찰) | 테스트(pose 'cook', lookAt 화덕) + 브라우저 화면. 사람 판단은 HR-014 |

추가: 화덕 하나에 요리사 둘이면 한 명만 예약한다(장기 규모 대비). 요리사가 아니면 조리하지 않는다.

## 검증

- `pnpm test` 346 통과(신규: cooking 13, 판단 1), lint(src·tests)·typecheck·format·build 통과.
  저장소 루트의 `eslint .` 는 사용자의 미추적 `.claude/helpers/*.cjs` 에서 오류가 나며 이번 변경과 무관하다.
- headless Chrome 148(`/?scene=kitchen-lab&view=3&orbit=0`, 배속 4): 09:09 "주방으로 가는 중" → 09:47 화덕 앞 "요리하는 중"
  (재료 예약 2, crop 4 그대로) → 10:45 crop 2·food 3, 곧바로 두 번째 조리. 화면에서 요리사가 화덕을 보고 팔을 내밀어 젓고
  화덕 위로 김이 오른다. 문 앞 시점에서 "주방" 라벨이 붙은 집으로 걸어간다(에이전트 관찰 — 사람의 판단이 아니다).

## 아직 없는 연동

- 감사 포인트 +3: TASK-035(GratitudeSystem). 저장 왕복: TASK-051(예약은 저장하지 않는다).
- 식사·식탁 위 음식 연출: TASK-032.
- 화덕으로 가는 경로가 없을 때는 0.5 초 재시도 간격으로 다시 시도한다(농사와 같음). 도달 불가 화덕 표시는 필요해지면 SleepSystem 방식으로 더한다.

## 다음 단계

1. TASK-032 식사
2. 사람 확인: HR-014, HR-013, HR-012, HR-001 / 006 / 011
