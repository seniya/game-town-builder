# STYLE-002 주민·플레이어 모형 전환

Date: 2026-09-25 22:50

## 대상과 상태

- 대상: TASK-STYLE-002 / 상태: **완료(에이전트 검증)**. 사람의 판단은 게이트 V2.
- 명세 MVP_SPEC 45.4, 결정 ADR 046(강체 스키닝 한 메시).

## 한 일

- `render/cuteCharacter.ts`(신설): 농부·요리사·목수·주민·플레이어의 둥근 SD 모형. 부품에 정점 색·뼈 번호를 넣어 한 지오메트리로 합치고
  `SkinnedMesh` 하나 + 외곽선 `SkinnedMesh` 하나로 그린다. 눈·입은 `unlit` 속성으로 같은 메시 안에서 명암 없이 그린다.
- `materials.ts`: 외곽선 셰이더가 스키닝·인스턴싱을 지원한다. `createModelToonMaterial`(툰 3 단·가장자리 밝힘·무조명 부위·π 밝기 규약).
- `NpcView`·`PlayerView`: 상자 모형 → 새 모형. 이불은 둥근 상자 두 겹. 모자 벗기는 모자 뼈를 0 으로 줄인다.
- 시안 장면에 시점 5 "마을 사람"(게임 캐릭터 다섯 나란히)을 더했다. `/?scene=style-lab&view=5&pose=idle|walk|cook|sit|work`.
- 빛 번짐 문턱 0.85 → 1.0: 흰 앞치마·요리 모자가 햇빛에서 번져 보였다(불꽃·해는 1 을 넘어 계속 번진다).
- `style/cuteFigure.ts` 의 `merge` 는 인덱스 없는 부품(둥근 상자)에 순차 인덱스를 붙인다.

## 측정 (headless, 1280 × 720, DPR 1, 10 초)

| 장면 | 평균 FPS | 최대 드로우콜 (STYLE-005 뒤 → 지금) |
| --- | --- | --- |
| `/?scene=kitchen-lab&orbit=0` | 60.0 | 236 → 116 |
| `/?scene=mvp-perf` | 60.0 | 416 → 302 |

사람 한 명이 상자 약 20 메시(+ 그림자 20) → 2 메시(+ 그림자 1)가 되었다.

## 관찰 (에이전트)

- 다섯이 모자·옷·소품으로 구별된다. 서기·걷기·조리·앉기·밭일 자세가 새 비율에서 땅에 붙는다(시안 장면 시점 5).
- 침실: 20 시 뒤 농부가 침대에 누워 이불을 덮는다. 큰 머리가 베개 위로 조금 높게 보인다(눕기 뿌리 이동은 V2 의견을 보고 다듬는다).

## 다음 할 일

- STYLE-003: 가구·소품 모형(InstancedMesh + 외곽선)과 가구 아이콘. 45.6 을 먼저 적는다.
